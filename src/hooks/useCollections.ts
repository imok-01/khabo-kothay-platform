import { collections, getCollection } from '../data/collections';

/**
 * Editorial-collections adapter — the hooks-layer seam for the curated
 * collection seed. Home imports from here, never from the raw dataset.
 */
export { collections, getCollection };
